extends@: default

form:
  fields:
    tabs:
      fields:
        info:
          type: tab
          title: Info
          fields:
            header.dtend:
              label: Datum und Zeit
              type: datetime
              format: Y-m-d H:i
          import@:
            type: partials/image
            context: theme://blueprints