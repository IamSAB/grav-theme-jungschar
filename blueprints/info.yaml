extends@: default

form:
  fields:
    tabs:
      fields:
        info:
          type: tab
          title: Info
          import@:
            type: partials/subtitle
            context: theme://blueprints
          fields:
            header.dtend:
              label: Datum und Zeit
              type: datetime
              format: Y-m-d H:i