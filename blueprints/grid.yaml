extends@: default

form:
  fields:
    tabs:
      fields:

        columns:
          type: tab
          title: Layout
          fields:

            columns:
              type: columns
              fields:

                column1:
                  type: column
                  fields:

                    header_classes:
                      type: section
                      title: CSS Klassen
                      underline: true

                    header.columns_classes:
                      label: Spalten
                      type: text

                    header.content_classes:
                      label: Inhalt
                      type: text