extends@: default

form:
  fields:
    tabs:
      fields:

        article:
          type: tab
          title: Layout
          fields:

            columns:
              type: columns
              fields:

                column1:
                  type: column
                  fields:

                    header_classes:
                      type: section
                      title: CSS Klassen
                      underline: true

                    header.column_classes:
                      label: Spalte
                      type: text
                      description: Genutzt, falls Artikel in einem Spaltenlayout verwendet wird

                    header.article_classes:
                      label: Artikel
                      type: text

                    header.content_classes:
                      label: Inhalt
                      type: text