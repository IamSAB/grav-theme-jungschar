{
  "scripts": {
    "dev": "npx tailwindcss -i ./input.css -o ./css/tailwind.css --watch",
    "build": "NODE_ENV=production npx tailwindcss -i ./input.css -o ./css/tailwind.min.css --minify"
  },
  "devDependencies": {
    "daisyui": "^1.16.5",
    "tailwind-heropatterns": "^0.0.8",
    "tailwindcss": "^3.0.2",
    "@tailwindcss/typography": "^0.5.0"
  }
}
