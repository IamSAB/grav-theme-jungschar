{% set image = page.header.image ? page.media.images[page.header.image] : page.media.images|first %}

{% switch page.header.status %}
	{% case 'teilzeit' %}
		{% set status = 'ring-warning' %}
	{% case 'pause' %}
		{% set status = 'ring-error' %}
	{% default %}
		{% set status = 'ring-success' %}
{% endswitch %}

<div class="flex flex-col items-center">
	<div class="avatar">
		<div class="rounded-full w-3/4 mx-auto ring {{ status }} ring-offset-base-100 ring-offset-2">
			{{ image.cropZoom(200,200).html|raw }}
		</div>
	</div>
	<h4 class="font-md font-bold mt-3">{{ page.title }}</h4>
	<div class="font-light">{{ page.header.name }}</div>
</div>
